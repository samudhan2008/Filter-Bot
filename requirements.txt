pyrofork==2.3.45
tgcrypto
# motor 2.5.1 + umongo 3.0.1 use asyncio.coroutine, which Python removed in
# 3.11 -- this is why the Dockerfile is pinned to python:3.10-slim. If you
# ever bump the Python base image past 3.10, these two need upgrading
# together (motor>=3.0 needs pymongo>=4, which needs a newer umongo release
# too -- check compatibility before touching any of the three).
motor==2.5.1
pymongo[srv]==3.12.3
umongo==3.0.1
marshmallow==3.14.1
dnspython==2.3.0
aiohttp
requests
pytz
Pillow
# No new hard dependencies were needed for the reliability/caching/
# multi-client improvements — retry, circuit breaker, TTL cache, and
# poster-on-disk caching are all stdlib-only.
